import React from 'react';
import { View } from 'react-native';

// Create this new Component instead of using View
// because of reusing styles

const Card = () => {
    return (
        <View></View>
    );
};

export default Card;
